# **WEBAPP- Simple web-app/Docker/Kubernetes(EKS)**
Webapp folder contains app code, docker file and deployment yml file to deploy and run pods in eks
- To build a docker file and push into docker hub
- After building docker file we can pull that and run in eks 
